export default class Marketing {
    update( {id, userName} ){
        //Tratamento de erro em cada Observer
        console.log(`[${id}]: [marketing will send an welcome email to ${userName}]`)
    }
}